require 'open-uri'
require 'json'

item do
  expected do
    static
    url = 'https://www.kernel.org/releases.json'
    set JSON.parse(open(url).read)['latest_stable']['version']
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
